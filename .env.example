# Application Configuration
NODE_ENV=development
PORT=3000

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Base URL of deployment for building URLs
# Development: http://localhost:3000
# Production: https://your-domain.com or http://your-azure-vm-dns-name.eastus.cloudapp.azure.com
BASE_URL=http://localhost:3000

# Database Configuration
# Leave empty to use default locations:
#   - Development: ./sqlite.db (current directory)
#   - Production: /datadrive/vid2story/data/sqlite.db
# Or specify a custom path:
# DATABASE_PATH=/custom/path/to/sqlite.db

# Uploads Directory
# Leave empty to use default: ./uploads
# For production on Azure, consider: /datadrive/vid2story/uploads
# UPLOADS_DIR=/datadrive/vid2story/uploads

# land2port Configuration (for video processing)
# Development (macOS): "/Users/username/src/clones/land2port"
# Production (Azure): "/datadrive/land2port"
LAND2PORT_PATH="/Users/foo/src/clones/land2port"

# Device for land2port processing
# Options: "coreml" (macOS), "cuda" (NVIDIA GPU), "cpu" (fallback)
# Development (macOS): "coreml"
# Production (Azure GPU VM): "cuda"
LAND2PORT_DEVICE="coreml"
